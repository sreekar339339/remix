BREAKING CHANGE: Rename the host-element `eventSource` prop to `on`, so event-aware elements read naturally (`<select on={events.people}>`). The `on` prop name was already reserved by the renderer (the removed host-event prop era left it guarded from DOM patching), so the change is a pure rename: update JSX props from `eventSource={...}` to `on={...}` everywhere. `EventSource`, `EventSourceProtocol`, `EventSourceInput`, and the `EVENT_SOURCE` brand are unchanged.
